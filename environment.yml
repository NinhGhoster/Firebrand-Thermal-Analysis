name: firebrand-thermal
channels:
  - defaults
dependencies:
  - python=3.12
  - pip
  - pytest
  - pip:
      - opencv-python-headless
      - numpy
      - pillow
      - customtkinter
      - darkdetect
      - click
      - netCDF4
      - pyinstaller
