name: firebrand-thermal
channels:
  - defaults
dependencies:
  - python=3.12
  - pip
  - pytest
  - pip:
      - opencv-python-headless
      - "numpy<2"
      - pillow
      - customtkinter
      - darkdetect
      - click
      - netCDF4
      - pyinstaller
